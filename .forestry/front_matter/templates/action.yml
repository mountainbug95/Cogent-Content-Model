---
label: Action
is_partial: true
display_field: label
fields:
  - type: text
    name: label
    label: Button Label
    description: The text displayed inside the action button
  - type: text
    name: url
    label: URL
  - type: boolean
    name: is_primary
    label: Is Primary
    description: Gives the button a primary color scheme
    default: false
  - type: boolean
    name: is_scrolly
    label: Is Scrolly
    description: Gives the button a scrolly animation (if a section ID is used in the URL)
    default: false
